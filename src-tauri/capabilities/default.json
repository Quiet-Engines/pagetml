{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + native open dialog for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "core:event:default"]
}
